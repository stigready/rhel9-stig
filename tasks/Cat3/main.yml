# Source: ComplianceAsCode/content (scap-security-guide) — BSD-3-Clause
# Imported by StigForge; cloud-breaker tasks stripped per policy/cloud-ami.yml
---
- block:
  - name: Ensure rng-tools is installed
    ansible.builtin.package:
      name: rng-tools
      state: present
    when: ( "kernel-core" in ansible_facts.packages )
    tags:
    - CCE-83504-1
    - DISA-STIG-RHEL-09-215090
    - enable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_rng-tools_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rng-tools is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rng-tools is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure policycoreutils is installed
    ansible.builtin.package:
      name: policycoreutils
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-84071-0
    - DISA-STIG-RHEL-09-431025
    - enable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_policycoreutils_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure policycoreutils is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure policycoreutils is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_package_policycoreutils_installed | default(true)
    | bool)
- block:
  - name: 'Uninstall nfs-utils Package: Ensure nfs-utils is removed'
    ansible.builtin.package:
      name: nfs-utils
      state: absent
    tags:
    - CCE-84243-5
    - DISA-STIG-RHEL-09-215025
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_nfs-utils_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall nfs-utils Package: Ensure nfs-utils is removed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall nfs-utils Package: Ensure nfs-utils is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Get rules groups
    ansible.builtin.shell: 'set -o pipefail

      LC_ALL=C grep "^[A-Z][A-Za-z_]*" /etc/aide.conf | grep -v "^ALLXTRAHASHES" | cut -f1 -d ''='' | tr -d '' '' | sort -u
      || true

      '
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '''aide'' in ansible_facts.packages'
    register: find_rules_groups_results
    changed_when: false
    check_mode: false
    tags:
    - CCE-90837-6
    - DISA-STIG-RHEL-09-651030
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - aide_verify_acls
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get rules groups'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get rules groups'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_aide_verify_acls | default(true) | bool)
- block:
  - name: Ensure the acl rule is present when aide is installed.
    ansible.builtin.replace:
      path: /etc/aide.conf
      regexp: (^\s*{{ item }}\s*=\s*)(?!.*acl)([^\s]*)
      replace: \g<1>\g<2>+acl
    when:
    - '"kernel-core" in ansible_facts.packages'
    - find_rules_groups_results is not skipped and 'aide' in ansible_facts.packages
    with_items: '{{ find_rules_groups_results.stdout_lines | map(''trim'') | list }}'
    tags:
    - CCE-90837-6
    - DISA-STIG-RHEL-09-651030
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - aide_verify_acls
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the acl rule is present when aide is installed.'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the acl rule is present when aide is installed.'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_aide_verify_acls | default(true) | bool)
- block:
  - name: Ensure the acl attribute is present on selection lines with direct attributes
    ansible.builtin.replace:
      path: /etc/aide.conf
      regexp: ^(\/\S+\s+)(?!.*acl)([a-z][a-zA-Z0-9+]*)
      replace: \g<1>\g<2>+acl
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '''aide'' in ansible_facts.packages'
    tags:
    - CCE-90837-6
    - DISA-STIG-RHEL-09-651030
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - aide_verify_acls
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the acl attribute is present on selection lines with direct attributes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the acl attribute is present on selection lines with direct attributes'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_aide_verify_acls | default(true) | bool)
- block:
  - name: Get rules groups
    ansible.builtin.shell: 'set -o pipefail

      LC_ALL=C grep "^[A-Z][A-Za-z_]*" /etc/aide.conf | grep -v "^ALLXTRAHASHES" | cut -f1 -d ''='' | tr -d '' '' | sort -u
      || true

      '
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '''aide'' in ansible_facts.packages'
    register: find_rules_groups_results
    changed_when: false
    check_mode: false
    tags:
    - CCE-83439-0
    - DISA-STIG-RHEL-09-651035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - aide_verify_ext_attributes
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get rules groups'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get rules groups'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_aide_verify_ext_attributes | default(true) | bool)
- block:
  - name: Ensure the xattrs rule is present when aide is installed.
    ansible.builtin.replace:
      path: /etc/aide.conf
      regexp: (^\s*{{ item }}\s*=\s*)(?!.*xattrs)([^\s]*)
      replace: \g<1>\g<2>+xattrs
    when:
    - '"kernel-core" in ansible_facts.packages'
    - find_rules_groups_results is not skipped and 'aide' in ansible_facts.packages
    with_items: '{{ find_rules_groups_results.stdout_lines | map(''trim'') | list }}'
    tags:
    - CCE-83439-0
    - DISA-STIG-RHEL-09-651035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - aide_verify_ext_attributes
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the xattrs rule is present when aide is installed.'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the xattrs rule is present when aide is installed.'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_aide_verify_ext_attributes | default(true) | bool)
- block:
  - name: Ensure the xattrs attribute is present on selection lines with direct attributes
    ansible.builtin.replace:
      path: /etc/aide.conf
      regexp: ^(\/\S+\s+)(?!.*xattrs)([a-z][a-zA-Z0-9+]*)
      replace: \g<1>\g<2>+xattrs
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '''aide'' in ansible_facts.packages'
    tags:
    - CCE-83439-0
    - DISA-STIG-RHEL-09-651035
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - aide_verify_ext_attributes
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the xattrs attribute is present on selection lines with direct attributes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the xattrs attribute is present on selection lines with direct attributes'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_aide_verify_ext_attributes | default(true) | bool)
- block:
  - name: Disable GNOME3 Automounting - autorun-never
    community.general.ini_file:
      mode: 420
      dest: /etc/dconf/db/local.d/00-security-settings
      section: org/gnome/desktop/media-handling
      option: autorun-never
      value: 'true'
      create: true
      no_extra_spaces: true
    register: result_ini
    when: '"gdm" in ansible_facts.packages'
    tags:
    - CCE-90257-7
    - DISA-STIG-RHEL-09-271030
    - DISA-STIG-RHEL-09-271035
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_autorun
    - low_complexity
    - low_severity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable GNOME3 Automounting - autorun-never'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GNOME3 Automounting - autorun-never'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_autorun | default(true) | bool)
- block:
  - name: Prevent user modification of GNOME3 Automounting - autorun-never
    ansible.builtin.lineinfile:
      mode: 420
      path: /etc/dconf/db/local.d/locks/00-security-settings-lock
      regexp: ^/org/gnome/desktop/media-handling/autorun-never$
      line: /org/gnome/desktop/media-handling/autorun-never
      create: true
    register: result_lineinfile
    when: '"gdm" in ansible_facts.packages'
    tags:
    - CCE-90257-7
    - DISA-STIG-RHEL-09-271030
    - DISA-STIG-RHEL-09-271035
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_autorun
    - low_complexity
    - low_severity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Prevent user modification of GNOME3 Automounting - autorun-never'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Prevent user modification of GNOME3 Automounting - autorun-never'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_autorun | default(true) | bool)
- block:
  - name: Dconf Update
    ansible.builtin.command: dconf update
    when:
    - '"gdm" in ansible_facts.packages'
    - result_ini is changed or result_lineinfile is changed
    tags:
    - CCE-90257-7
    - DISA-STIG-RHEL-09-271030
    - DISA-STIG-RHEL-09-271035
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_autorun
    - low_complexity
    - low_severity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Dconf Update'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Dconf Update'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_autorun | default(true) | bool)
- block:
  - name: Ensure dnf Removes Previous Package Versions - Ensure DNF Removes Previous Package Versions
    ansible.builtin.lineinfile:
      dest: /etc/dnf/dnf.conf
      regexp: ^#?clean_requirements_on_remove
      line: clean_requirements_on_remove=1
      insertafter: \[main\]
      create: true
      mode: 420
    when: ( not ( "kernel-core" in ansible_facts.packages and "bootc" in ansible_facts.packages and not "openshift-kubelet"
      in ansible_facts.packages and "ostree" in ansible_proc_cmdline ) and "dnf" in ansible_facts.packages )
    tags:
    - CCE-83458-0
    - DISA-STIG-RHEL-09-214035
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-11(a)
    - NIST-800-53-CM-11(b)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-2(6)
    - clean_components_post_updating
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure dnf Removes Previous Package Versions - Ensure DNF Removes Previous
      Package Versions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure dnf Removes Previous Package Versions - Ensure DNF Removes Previous Package Versions'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_clean_components_post_updating | default(true) | bool)
- block:
  - name: Find /etc/security/limits.d files containing maxlogins configuration
    ansible.builtin.find:
      paths: /etc/security/limits.d
      contains: ^[\s]*\*[\s]+(?:(?:hard)|(?:-))[\s]+maxlogins
      patterns: '*.conf'
    register: maxlogins
    when: ( "pam" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )
    tags:
    - CCE-83641-1
    - CJIS-5.5.2.2
    - DISA-STIG-RHEL-09-412040
    - NIST-800-53-AC-10
    - NIST-800-53-CM-6(a)
    - accounts_max_concurrent_login_sessions
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/security/limits.d files containing maxlogins configuration'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/security/limits.d files containing maxlogins configuration'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_accounts_max_concurrent_login_sessions | default(true)
    | bool)
- block:
  - name: Limit the Number of Concurrent Login Sessions Allowed Per User in files from limits.d
    ansible.builtin.replace:
      dest: '{{ item.path }}'
      regexp: ^#?\*.*maxlogins.*
      replace: '*          hard    maxlogins     {{ var_accounts_max_concurrent_login_sessions }}'
    with_items:
    - '{{ maxlogins.files }}'
    when: ( "pam" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )
    tags:
    - CCE-83641-1
    - CJIS-5.5.2.2
    - DISA-STIG-RHEL-09-412040
    - NIST-800-53-AC-10
    - NIST-800-53-CM-6(a)
    - accounts_max_concurrent_login_sessions
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Limit the Number of Concurrent Login Sessions Allowed Per User in files from
      limits.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit the Number of Concurrent Login Sessions Allowed Per User in files from limits.d'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_accounts_max_concurrent_login_sessions | default(true)
    | bool)
- block:
  - name: Limit the Number of Concurrent Login Sessions Allowed Per User
    ansible.builtin.lineinfile:
      state: present
      dest: /etc/security/limits.conf
      insertbefore: ^# End of file
      regexp: ^#?\*.*maxlogins
      line: '*          hard    maxlogins     {{ var_accounts_max_concurrent_login_sessions }}'
      create: true
      mode: 420
    when:
    - ( "pam" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )
    - maxlogins.matched == 0
    tags:
    - CCE-83641-1
    - CJIS-5.5.2.2
    - DISA-STIG-RHEL-09-412040
    - NIST-800-53-AC-10
    - NIST-800-53-CM-6(a)
    - accounts_max_concurrent_login_sessions
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Limit the Number of Concurrent Login Sessions Allowed Per User'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit the Number of Concurrent Login Sessions Allowed Per User'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_accounts_max_concurrent_login_sessions | default(true)
    | bool)
- block:
  - name: Check if pti argument is already present in /etc/default/grub
    ansible.builtin.slurp:
      src: /etc/default/grub
    register: etc_default_grub
    when: ( "grub2-common" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )
    tags:
    - CCE-83843-3
    - DISA-STIG-RHEL-09-212050
    - NIST-800-53-SI-16
    - grub2_pti_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if pti argument is already present in /etc/default/grub'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if pti argument is already present in /etc/default/grub'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_pti_argument | default(true) | bool)
- block:
  - name: Check if pti argument is already present
    ansible.builtin.command: /sbin/grubby --info=ALL
    register: grubby_info
    check_mode: false
    changed_when: false
    failed_when: false
    when: ( "grub2-common" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )
    tags:
    - CCE-83843-3
    - DISA-STIG-RHEL-09-212050
    - NIST-800-53-SI-16
    - grub2_pti_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if pti argument is already present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if pti argument is already present'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_pti_argument | default(true) | bool)
- block:
  - name: Update grub defaults and the bootloader menu
    ansible.builtin.command: /sbin/grubby --update-kernel=ALL --args="pti=on"
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )
    - (grubby_info.stdout is not search('pti=on')) or ((etc_default_grub['content'] | b64decode) is not search('pti=on'))
    tags:
    - CCE-83843-3
    - DISA-STIG-RHEL-09-212050
    - NIST-800-53-SI-16
    - grub2_pti_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Update grub defaults and the bootloader menu'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Update grub defaults and the bootloader menu'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_pti_argument | default(true) | bool)
- block:
  - name: Ensure kernel module 'firewire-core' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/firewire-core.conf
      regexp: install\s+firewire-core
      line: install firewire-core /bin/false
      mode: 420
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-84060-3
    - DISA-STIG-RHEL-09-213055
    - NIST-800-53-AC-18
    - disable_strategy
    - kernel_module_firewire-core_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''firewire-core'' is disabled" (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''firewire-core'' is disabled" raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure kernel module 'firewire-core' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/firewire-core.conf
      regexp: ^blacklist firewire-core$
      line: blacklist firewire-core
      mode: 420
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-84060-3
    - DISA-STIG-RHEL-09-213055
    - NIST-800-53-AC-18
    - disable_strategy
    - kernel_module_firewire-core_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''firewire-core'' is blacklisted" (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''firewire-core'' is blacklisted" raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Ensure kernel module 'tipc' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/tipc.conf
      regexp: install\s+tipc
      line: install tipc /bin/false
      mode: 420
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-84065-2
    - DISA-STIG-RHEL-09-213065
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_tipc_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''tipc'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''tipc'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_kernel_module_tipc_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'tipc' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/tipc.conf
      regexp: ^blacklist tipc$
      line: blacklist tipc
      mode: 420
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-84065-2
    - DISA-STIG-RHEL-09-213065
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_tipc_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''tipc'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''tipc'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_kernel_module_tipc_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'cramfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/cramfs.conf
      regexp: install\s+cramfs
      line: install cramfs /bin/false
      mode: 420
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83853-2
    - DISA-STIG-RHEL-09-231195
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_cramfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''cramfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''cramfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_kernel_module_cramfs_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'cramfs' is blacklisted
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/cramfs.conf
      regexp: ^blacklist cramfs$
      line: blacklist cramfs
      mode: 420
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83853-2
    - DISA-STIG-RHEL-09-231195
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_cramfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''cramfs'' is blacklisted" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''cramfs'' is blacklisted" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_kernel_module_cramfs_disabled | default(true) | bool)
- block:
  - name: Restrict Access to Kernel Message Buffer - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83952-2
    - DISA-STIG-RHEL-09-213010
    - NIST-800-171-3.1.5
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_dmesg_restrict
  rescue:
  - name: 'StigForge: continue after error in ''Restrict Access to Kernel Message Buffer - Set fact for sysctl paths'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict Access to Kernel Message Buffer - Set fact for sysctl paths'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_dmesg_restrict | default(true) | bool)
- block:
  - name: Restrict Access to Kernel Message Buffer - Find all files that contain kernel.dmesg_restrict
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.dmesg_restrict\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83952-2
    - DISA-STIG-RHEL-09-213010
    - NIST-800-171-3.1.5
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_dmesg_restrict
  rescue:
  - name: 'StigForge: continue after error in ''Restrict Access to Kernel Message Buffer - Find all files that contain kernel.dmesg_restrict''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict Access to Kernel Message Buffer - Find all files that contain kernel.dmesg_restrict'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_dmesg_restrict | default(true) | bool)
- block:
  - name: Restrict Access to Kernel Message Buffer - Find all files that set kernel.dmesg_restrict to correct value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.dmesg_restrict\s*=\s*1$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83952-2
    - DISA-STIG-RHEL-09-213010
    - NIST-800-171-3.1.5
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_dmesg_restrict
  rescue:
  - name: 'StigForge: continue after error in ''Restrict Access to Kernel Message Buffer - Find all files that set kernel.dmesg_restrict
      to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict Access to Kernel Message Buffer - Find all files that set kernel.dmesg_restrict to correct
        value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_dmesg_restrict | default(true) | bool)
- block:
  - name: Restrict Access to Kernel Message Buffer - Comment out any occurrences of kernel.dmesg_restrict from config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*kernel.dmesg_restrict
      replace: '#kernel.dmesg_restrict'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"kernel-core" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CCE-83952-2
    - DISA-STIG-RHEL-09-213010
    - NIST-800-171-3.1.5
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_dmesg_restrict
  rescue:
  - name: 'StigForge: continue after error in ''Restrict Access to Kernel Message Buffer - Comment out any occurrences of
      kernel.dmesg_restrict from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict Access to Kernel Message Buffer - Comment out any occurrences of kernel.dmesg_restrict from
        config files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_dmesg_restrict | default(true) | bool)
- block:
  - name: Restrict Access to Kernel Message Buffer - Comment out any occurrences of kernel.dmesg_restrict from /etc/sysctl.conf
    ansible.builtin.replace:
      path: '{{ item }}'
      regexp: ^[\s]*kernel.dmesg_restrict
      replace: '#kernel.dmesg_restrict'
    with_fileglob:
    - /etc/sysctl.conf
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83952-2
    - DISA-STIG-RHEL-09-213010
    - NIST-800-171-3.1.5
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_dmesg_restrict
  rescue:
  - name: 'StigForge: continue after error in ''Restrict Access to Kernel Message Buffer - Comment out any occurrences of
      kernel.dmesg_restrict from /etc/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict Access to Kernel Message Buffer - Comment out any occurrences of kernel.dmesg_restrict from
        /etc/sysctl.conf'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_dmesg_restrict | default(true) | bool)
- block:
  - name: Restrict Access to Kernel Message Buffer - Ensure sysctl kernel.dmesg_restrict is set to 1
    ansible.posix.sysctl:
      name: kernel.dmesg_restrict
      value: '1'
      sysctl_file: /etc/sysctl.d/kernel_dmesg_restrict.conf
      state: present
      reload: true
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83952-2
    - DISA-STIG-RHEL-09-213010
    - NIST-800-171-3.1.5
    - NIST-800-53-SI-11(a)
    - NIST-800-53-SI-11(b)
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_dmesg_restrict
  rescue:
  - name: 'StigForge: continue after error in ''Restrict Access to Kernel Message Buffer - Ensure sysctl kernel.dmesg_restrict
      is set to 1'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Restrict Access to Kernel Message Buffer - Ensure sysctl kernel.dmesg_restrict is set to 1'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_dmesg_restrict | default(true) | bool)
- block:
  - name: Disallow kernel profiling by unprivileged users - Set fact for sysctl paths
    ansible.builtin.set_fact:
      sysctl_paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83959-7
    - DISA-STIG-RHEL-09-213015
    - NIST-800-53-AC-6
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_perf_event_paranoid
  rescue:
  - name: 'StigForge: continue after error in ''Disallow kernel profiling by unprivileged users - Set fact for sysctl paths''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disallow kernel profiling by unprivileged users - Set fact for sysctl paths'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_perf_event_paranoid | default(true)
    | bool)
- block:
  - name: Disallow kernel profiling by unprivileged users - Find all files that contain kernel.perf_event_paranoid
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.perf_event_paranoid\s*=\s*.*$'
    register: find_all_values
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83959-7
    - DISA-STIG-RHEL-09-213015
    - NIST-800-53-AC-6
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_perf_event_paranoid
  rescue:
  - name: 'StigForge: continue after error in ''Disallow kernel profiling by unprivileged users - Find all files that contain
      kernel.perf_event_paranoid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disallow kernel profiling by unprivileged users - Find all files that contain kernel.perf_event_paranoid''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_perf_event_paranoid | default(true)
    | bool)
- block:
  - name: Disallow kernel profiling by unprivileged users - Find all files that set kernel.perf_event_paranoid to correct
      value
    ansible.builtin.shell:
      cmd: find -L {{ sysctl_paths | join(" ") }} -type f -name '*.conf' | xargs grep -HP '^\s*kernel.perf_event_paranoid\s*=\s*2$'
    register: find_correct_value
    check_mode: false
    changed_when: false
    failed_when: false
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83959-7
    - DISA-STIG-RHEL-09-213015
    - NIST-800-53-AC-6
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_perf_event_paranoid
  rescue:
  - name: 'StigForge: continue after error in ''Disallow kernel profiling by unprivileged users - Find all files that set
      kernel.perf_event_paranoid to correct value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disallow kernel profiling by unprivileged users - Find all files that set kernel.perf_event_paranoid
        to correct value'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_perf_event_paranoid | default(true)
    | bool)
- block:
  - name: Disallow kernel profiling by unprivileged users - Comment out any occurrences of kernel.perf_event_paranoid from
      config files
    ansible.builtin.replace:
      path: '{{ item | split(":") | first }}'
      regexp: ^[\s]*kernel.perf_event_paranoid
      replace: '#kernel.perf_event_paranoid'
    loop: '{{ find_all_values.stdout_lines }}'
    when:
    - '"kernel-core" in ansible_facts.packages'
    - find_correct_value.stdout_lines | length == 0 or find_all_values.stdout_lines | length > find_correct_value.stdout_lines
      | length
    tags:
    - CCE-83959-7
    - DISA-STIG-RHEL-09-213015
    - NIST-800-53-AC-6
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_perf_event_paranoid
  rescue:
  - name: 'StigForge: continue after error in ''Disallow kernel profiling by unprivileged users - Comment out any occurrences
      of kernel.perf_event_paranoid from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disallow kernel profiling by unprivileged users - Comment out any occurrences of kernel.perf_event_paranoid
        from config files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_perf_event_paranoid | default(true)
    | bool)
- block:
  - name: Disallow kernel profiling by unprivileged users - Comment out any occurrences of kernel.perf_event_paranoid from
      /etc/sysctl.conf
    ansible.builtin.replace:
      path: '{{ item }}'
      regexp: ^[\s]*kernel.perf_event_paranoid
      replace: '#kernel.perf_event_paranoid'
    with_fileglob:
    - /etc/sysctl.conf
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83959-7
    - DISA-STIG-RHEL-09-213015
    - NIST-800-53-AC-6
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_perf_event_paranoid
  rescue:
  - name: 'StigForge: continue after error in ''Disallow kernel profiling by unprivileged users - Comment out any occurrences
      of kernel.perf_event_paranoid from /etc/sysctl.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disallow kernel profiling by unprivileged users - Comment out any occurrences of kernel.perf_event_paranoid
        from /etc/sysctl.conf'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_perf_event_paranoid | default(true)
    | bool)
- block:
  - name: Disallow kernel profiling by unprivileged users - Ensure sysctl kernel.perf_event_paranoid is set to 2
    ansible.posix.sysctl:
      name: kernel.perf_event_paranoid
      value: '2'
      sysctl_file: /etc/sysctl.d/kernel_perf_event_paranoid.conf
      state: present
      reload: true
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83959-7
    - DISA-STIG-RHEL-09-213015
    - NIST-800-53-AC-6
    - disable_strategy
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
    - sysctl_kernel_perf_event_paranoid
  rescue:
  - name: 'StigForge: continue after error in ''Disallow kernel profiling by unprivileged users - Ensure sysctl kernel.perf_event_paranoid
      is set to 2'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disallow kernel profiling by unprivileged users - Ensure sysctl kernel.perf_event_paranoid is set
        to 2'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sysctl_kernel_perf_event_paranoid | default(true)
    | bool)
- block:
  - name: Disable chrony daemon from acting as server
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/chrony.conf
        create: true
        regexp: (?i)^\s*port\s+
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/chrony.conf
      ansible.builtin.lineinfile:
        path: /etc/chrony.conf
        create: true
        regexp: (?i)^\s*port\s+
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/chrony.conf
      ansible.builtin.lineinfile:
        path: /etc/chrony.conf
        create: true
        regexp: (?i)^\s*port\s+
        line: port 0
        state: present
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - CCE-87543-5
    - DISA-STIG-RHEL-09-252025
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)
    - chronyd_client_only
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable chrony daemon from acting as server'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable chrony daemon from acting as server'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_chronyd_client_only | default(true) | bool)
- block:
  - name: Configure chrony-wait.service to use Unix socket - Check if chrony-wait.service exists
    ansible.builtin.stat:
      path: /usr/lib/systemd/system/chrony-wait.service
    register: chrony_wait_service
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - CCE-86468-6
    - DISA-STIG-RHEL-09-252030
    - NIST-800-53-CM-7(1)
    - chronyd_configure_local_socket
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure chrony-wait.service to use Unix socket - Check if chrony-wait.service
      exists'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure chrony-wait.service to use Unix socket - Check if chrony-wait.service exists'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_chronyd_configure_local_socket | default(true) | bool)
- block:
  - name: Configure chrony-wait.service to use Unix socket - Replace chrony-wait.service to use Unix socket (KCS 7064388)
    ansible.builtin.copy:
      dest: /etc/systemd/system/chrony-wait.service
      mode: '0644'
      content: '[Unit]

        Description=Wait for chrony to synchronize system clock(KCS 7064388)

        Documentation=man:chronyc(1)

        After=chronyd.service

        Requires=chronyd.service

        Before=time-sync.target

        Wants=time-sync.target


        [Service]

        Type=oneshot

        # Wait for chronyd to update the clock and the remaining

        # correction to be less than 0.1 seconds

        ExecStart=/usr/bin/chronyc waitsync 0 0.1 0.0 1

        # Wait for at most 3 minutes

        TimeoutStartSec=180

        RemainAfterExit=yes

        StandardOutput=null


        CapabilityBoundingSet=~CAP_AUDIT_CONTROL CAP_AUDIT_READ CAP_AUDIT_WRITE

        CapabilityBoundingSet=~CAP_BLOCK_SUSPEND CAP_KILL CAP_LEASE CAP_LINUX_IMMUTABLE

        CapabilityBoundingSet=~CAP_MAC_ADMIN CAP_MAC_OVERRIDE CAP_MKNOD CAP_SYS_ADMIN

        CapabilityBoundingSet=~CAP_SYS_BOOT CAP_SYS_CHROOT CAP_SYS_MODULE CAP_SYS_PACCT

        CapabilityBoundingSet=~CAP_SYS_PTRACE CAP_SYS_RAWIO CAP_SYS_TTY_CONFIG CAP_WAKE_ALARM

        DevicePolicy=closed

        #DynamicUser=yes

        IPAddressAllow=localhost

        IPAddressDeny=any

        LockPersonality=yes

        MemoryDenyWriteExecute=yes

        PrivateDevices=yes

        ProcSubset=pid

        ProtectClock=yes

        ProtectControlGroups=yes

        ProtectHome=yes

        ProtectHostname=yes

        ProtectKernelLogs=yes

        ProtectKernelModules=yes

        ProtectKernelTunables=yes

        ProtectProc=invisible

        ProtectSystem=strict

        RestrictAddressFamilies=AF_UNIX

        RestrictNamespaces=yes

        RestrictRealtime=yes

        SystemCallArchitectures=native

        SystemCallFilter=@system-service

        SystemCallFilter=~@privileged @resources

        UMask=0777


        [Install]

        WantedBy=multi-user.target

        '
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    - (chrony_wait_service is not skipped and chrony_wait_service.stat.exists)
    tags:
    - CCE-86468-6
    - DISA-STIG-RHEL-09-252030
    - NIST-800-53-CM-7(1)
    - chronyd_configure_local_socket
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure chrony-wait.service to use Unix socket - Replace chrony-wait.service
      to use Unix socket (KCS 7064388)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure chrony-wait.service to use Unix socket - Replace chrony-wait.service to use Unix socket
        (KCS 7064388)'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_chronyd_configure_local_socket | default(true) | bool)
- block:
  - name: Configure chrony-wait.service to use Unix socket - Reload systemd daemon and enable chrony-wait.service
    ansible.builtin.systemd:
      name: chrony-wait.service
      daemon_reload: true
      enabled: true
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    - (chrony_wait_service is not skipped and chrony_wait_service.stat.exists)
    tags:
    - CCE-86468-6
    - DISA-STIG-RHEL-09-252030
    - NIST-800-53-CM-7(1)
    - chronyd_configure_local_socket
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure chrony-wait.service to use Unix socket - Reload systemd daemon and
      enable chrony-wait.service'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure chrony-wait.service to use Unix socket - Reload systemd daemon and enable chrony-wait.service''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_chronyd_configure_local_socket | default(true) | bool)
- block:
  - name: Disable network management of chrony daemon
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/chrony.conf
        create: true
        regexp: (?i)^\s*cmdport\s+
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/chrony.conf
      ansible.builtin.lineinfile:
        path: /etc/chrony.conf
        create: true
        regexp: (?i)^\s*cmdport\s+
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/chrony.conf
      ansible.builtin.lineinfile:
        path: /etc/chrony.conf
        create: true
        regexp: (?i)^\s*cmdport\s+
        line: cmdport 0
        state: present
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - CCE-88876-8
    - DISA-STIG-RHEL-09-252030
    - NIST-800-53-CM-7(1)
    - chronyd_no_chronyc_network
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable network management of chrony daemon'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable network management of chrony daemon'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_chronyd_no_chronyc_network | default(true) | bool)
- block:
  - name: Log USBGuard daemon audit events using Linux Audit
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/usbguard/usbguard-daemon.conf
        create: true
        regexp: (?i)^[ \\t]*AuditBackend=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/usbguard/usbguard-daemon.conf
      ansible.builtin.lineinfile:
        path: /etc/usbguard/usbguard-daemon.conf
        create: true
        regexp: (?i)^[ \\t]*AuditBackend=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/usbguard/usbguard-daemon.conf
      ansible.builtin.lineinfile:
        path: /etc/usbguard/usbguard-daemon.conf
        create: true
        regexp: (?i)^[ \\t]*AuditBackend=
        line: AuditBackend=LinuxAudit
        state: present
    when:
    - ( ansible_architecture != "s390x" and "kernel-core" in ansible_facts.packages )
    - '"usbguard" in ansible_facts.packages'
    tags:
    - CCE-84206-2
    - DISA-STIG-RHEL-09-291025
    - NIST-800-53-AU-2
    - NIST-800-53-CM-8(3)
    - NIST-800-53-IA-3
    - configure_strategy
    - configure_usbguard_auditbackend
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Log USBGuard daemon audit events using Linux Audit'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Log USBGuard daemon audit events using Linux Audit'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_usbguard_auditbackend | default(true) |
    bool)
- block:
  - name: Check if audit argument is already present in /etc/default/grub
    ansible.builtin.slurp:
      src: /etc/default/grub
    register: etc_default_grub
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - CCE-83651-0
    - CJIS-5.4.1.1
    - DISA-STIG-RHEL-09-212055
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-17(1)
    - NIST-800-53-AU-10
    - NIST-800-53-AU-14(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IR-5(1)
    - PCI-DSS-Req-10.3
    - PCI-DSSv4-10.7
    - PCI-DSSv4-10.7.2
    - grub2_audit_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if audit argument is already present in /etc/default/grub'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if audit argument is already present in /etc/default/grub'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_audit_argument | default(true) | bool)
- block:
  - name: Check if audit argument is already present
    ansible.builtin.command: /sbin/grubby --info=ALL
    register: grubby_info
    check_mode: false
    changed_when: false
    failed_when: false
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - CCE-83651-0
    - CJIS-5.4.1.1
    - DISA-STIG-RHEL-09-212055
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-17(1)
    - NIST-800-53-AU-10
    - NIST-800-53-AU-14(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IR-5(1)
    - PCI-DSS-Req-10.3
    - PCI-DSSv4-10.7
    - PCI-DSSv4-10.7.2
    - grub2_audit_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if audit argument is already present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if audit argument is already present'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_audit_argument | default(true) | bool)
- block:
  - name: Update grub defaults and the bootloader menu
    ansible.builtin.command: /sbin/grubby --update-kernel=ALL --args="audit=1"
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    - (grubby_info.stdout is not search('audit=1')) or ((etc_default_grub['content'] | b64decode) is not search('audit=1'))
    tags:
    - CCE-83651-0
    - CJIS-5.4.1.1
    - DISA-STIG-RHEL-09-212055
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-17(1)
    - NIST-800-53-AU-10
    - NIST-800-53-AU-14(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IR-5(1)
    - PCI-DSS-Req-10.3
    - PCI-DSSv4-10.7
    - PCI-DSSv4-10.7.2
    - grub2_audit_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Update grub defaults and the bootloader menu'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Update grub defaults and the bootloader menu'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_audit_argument | default(true) | bool)
- block:
  - name: Check if audit_backlog_limit argument is already present in /etc/default/grub
    ansible.builtin.slurp:
      src: /etc/default/grub
    register: etc_default_grub
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - CCE-83652-8
    - DISA-STIG-RHEL-09-653120
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-10.7
    - PCI-DSSv4-10.7.2
    - grub2_audit_backlog_limit_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if audit_backlog_limit argument is already present in /etc/default/grub''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if audit_backlog_limit argument is already present in /etc/default/grub'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_audit_backlog_limit_argument | default(true)
    | bool)
- block:
  - name: Check if audit_backlog_limit argument is already present
    ansible.builtin.command: /sbin/grubby --info=ALL
    register: grubby_info
    check_mode: false
    changed_when: false
    failed_when: false
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - CCE-83652-8
    - DISA-STIG-RHEL-09-653120
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-10.7
    - PCI-DSSv4-10.7.2
    - grub2_audit_backlog_limit_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if audit_backlog_limit argument is already present'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if audit_backlog_limit argument is already present'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_audit_backlog_limit_argument | default(true)
    | bool)
- block:
  - name: Update grub defaults and the bootloader menu
    ansible.builtin.command: /sbin/grubby --update-kernel=ALL --args="audit_backlog_limit={{ var_audit_backlog_limit }}"
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    - (grubby_info.stdout is not search('audit_backlog_limit=' ~ var_audit_backlog_limit)) or ((etc_default_grub['content']
      | b64decode) is not search('audit_backlog_limit=' ~ var_audit_backlog_limit))
    tags:
    - CCE-83652-8
    - DISA-STIG-RHEL-09-653120
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-10.7
    - PCI-DSSv4-10.7.2
    - grub2_audit_backlog_limit_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Update grub defaults and the bootloader menu'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Update grub defaults and the bootloader menu'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_grub2_audit_backlog_limit_argument | default(true)
    | bool)
- block:
  - name: Resolve information before writing to audit logs
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/audit/auditd.conf
        create: true
        regexp: (?i)(?i)^\s*log_format\s*=\s*
        mode: u-x,g-wx,o-rwx
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/audit/auditd.conf
      ansible.builtin.lineinfile:
        path: /etc/audit/auditd.conf
        create: true
        regexp: (?i)(?i)^\s*log_format\s*=\s*
        mode: u-x,g-wx,o-rwx
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/audit/auditd.conf
      ansible.builtin.lineinfile:
        path: /etc/audit/auditd.conf
        create: true
        regexp: (?i)(?i)^\s*log_format\s*=\s*
        mode: u-x,g-wx,o-rwx
        line: log_format = ENRICHED
        state: present
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83696-5
    - DISA-STIG-RHEL-09-653100
    - NIST-800-53-AU-3
    - NIST-800-53-CM-6
    - auditd_log_format
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Resolve information before writing to audit logs'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Resolve information before writing to audit logs'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_auditd_log_format | default(true) | bool)
