# Source: ComplianceAsCode/content (scap-security-guide) — BSD-3-Clause
# Imported by StigForge; cloud-breaker tasks stripped per policy/cloud-ami.yml
---
- block:
  - name: 'Uninstall vsftpd Package: Ensure vsftpd is removed'
    ansible.builtin.package:
      name: vsftpd
      state: absent
    tags:
    - CCE-84159-3
    - DISA-STIG-RHEL-09-215015
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-CM-7.1(ii)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(1).1(v)
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_vsftpd_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall vsftpd Package: Ensure vsftpd is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall vsftpd Package: Ensure vsftpd is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_package_vsftpd_removed | default(true) | bool)
- block:
  - name: 'Uninstall telnet-server Package: Ensure telnet-server is removed'
    ansible.builtin.package:
      name: telnet-server
      state: absent
    tags:
    - CCE-84149-4
    - DISA-STIG-RHEL-09-215040
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.2
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_telnet-server_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall telnet-server Package: Ensure telnet-server is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall telnet-server Package: Ensure telnet-server is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall tftp-server Package: Ensure tftp-server is removed'
    ansible.builtin.package:
      name: tftp-server
      state: absent
    tags:
    - CCE-84154-4
    - DISA-STIG-RHEL-09-215060
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_tftp-server_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall tftp-server Package: Ensure tftp-server is removed'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall tftp-server Package: Ensure tftp-server is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: Configure BIND to use System Crypto Policy - Check BIND configuration file exists
    ansible.builtin.stat:
      path: /etc/named.conf
    register: bind_config_file
    when: '"bind" in ansible_facts.packages'
    tags:
    - CCE-83451-5
    - DISA-STIG-RHEL-09-672050
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - configure_bind_crypto_policy
    - configure_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure BIND to use System Crypto Policy - Check BIND configuration file
      exists'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure BIND to use System Crypto Policy - Check BIND configuration file exists'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_bind_crypto_policy | default(true) | bool)
- block:
  - name: Configure BIND to use System Crypto Policy - Aborting remediation, file not found
    ansible.builtin.debug:
      msg: Aborting remediation as '/etc/named.conf' was not found.
    when:
    - '"bind" in ansible_facts.packages'
    - not (bind_config_file is not skipped and bind_config_file.stat.exists)
    tags:
    - CCE-83451-5
    - DISA-STIG-RHEL-09-672050
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - configure_bind_crypto_policy
    - configure_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure BIND to use System Crypto Policy - Aborting remediation, file not
      found'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure BIND to use System Crypto Policy - Aborting remediation, file not found'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_bind_crypto_policy | default(true) | bool)
- block:
  - name: Configure BIND to use System Crypto Policy - Insert crypto-policy into BIND config
    ansible.builtin.lineinfile:
      path: /etc/named.conf
      insertafter: ^\s*options\s*{
      line: ' include "/etc/crypto-policies/back-ends/bind.config";'
      state: present
    when:
    - '"bind" in ansible_facts.packages'
    - (bind_config_file is not skipped and bind_config_file.stat.exists)
    tags:
    - CCE-83451-5
    - DISA-STIG-RHEL-09-672050
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - configure_bind_crypto_policy
    - configure_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure BIND to use System Crypto Policy - Insert crypto-policy into BIND
      config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure BIND to use System Crypto Policy - Insert crypto-policy into BIND config'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_bind_crypto_policy | default(true) | bool)
- block:
  - name: Configure System Cryptography Policy - Check current crypto policy (runtime)
    ansible.builtin.command: /usr/bin/update-crypto-policies --show
    register: current_crypto_policy
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - CCE-83450-7
    - DISA-STIG-RHEL-09-215105
    - DISA-STIG-RHEL-09-672030
    - NIST-800-53-AC-17(2)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - configure_crypto_policy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure System Cryptography Policy - Check current crypto policy (runtime)''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure System Cryptography Policy - Check current crypto policy (runtime)'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_crypto_policy | default(true) | bool)
- block:
  - name: Configure System Cryptography Policy - Get mtime of /etc/crypto-policies/config
    ansible.builtin.stat:
      path: /etc/crypto-policies/config
    register: config_file_stat
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - CCE-83450-7
    - DISA-STIG-RHEL-09-215105
    - DISA-STIG-RHEL-09-672030
    - NIST-800-53-AC-17(2)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - configure_crypto_policy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure System Cryptography Policy - Get mtime of /etc/crypto-policies/config''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure System Cryptography Policy - Get mtime of /etc/crypto-policies/config'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_crypto_policy | default(true) | bool)
- block:
  - name: Configure System Cryptography Policy - Get mtime of /etc/crypto-policies/state/current
    ansible.builtin.stat:
      path: /etc/crypto-policies/state/current
    register: current_file_stat
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - CCE-83450-7
    - DISA-STIG-RHEL-09-215105
    - DISA-STIG-RHEL-09-672030
    - NIST-800-53-AC-17(2)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - configure_crypto_policy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure System Cryptography Policy - Get mtime of /etc/crypto-policies/state/current''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure System Cryptography Policy - Get mtime of /etc/crypto-policies/state/current'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_crypto_policy | default(true) | bool)
- block:
  - name: Configure System Cryptography Policy - Check existence of /etc/crypto-policies/back-ends/nss.config
    ansible.builtin.stat:
      path: /etc/crypto-policies/back-ends/nss.config
    register: nss_config_stat
    changed_when: false
    failed_when: false
    check_mode: false
    tags:
    - CCE-83450-7
    - DISA-STIG-RHEL-09-215105
    - DISA-STIG-RHEL-09-672030
    - NIST-800-53-AC-17(2)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - configure_crypto_policy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure System Cryptography Policy - Check existence of /etc/crypto-policies/back-ends/nss.config''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure System Cryptography Policy - Check existence of /etc/crypto-policies/back-ends/nss.config''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_crypto_policy | default(true) | bool)
- block:
  - name: Configure System Cryptography Policy - Verify that Crypto Policy is Set (runtime)
    ansible.builtin.command: /usr/bin/update-crypto-policies --set {{ var_system_crypto_policy }}
    when: (current_crypto_policy.stdout.strip() != var_system_crypto_policy) or ((config_file_stat is not skipped and config_file_stat.stat.exists)
      and (current_file_stat is not skipped and current_file_stat.stat.exists) and (config_file_stat is not skipped and config_file_stat.stat.mtime)
      > (current_file_stat is not skipped and current_file_stat.stat.mtime)) or (not (nss_config_stat is not skipped and nss_config_stat.stat.exists))
    tags:
    - CCE-83450-7
    - DISA-STIG-RHEL-09-215105
    - DISA-STIG-RHEL-09-672030
    - NIST-800-53-AC-17(2)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - configure_crypto_policy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure System Cryptography Policy - Verify that Crypto Policy is Set (runtime)''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure System Cryptography Policy - Verify that Crypto Policy is Set (runtime)'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_crypto_policy | default(true) | bool)
- block:
  - name: Configure Libreswan to use System Crypto Policy
    ansible.builtin.lineinfile:
      path: /etc/ipsec.conf
      line: include /etc/crypto-policies/back-ends/libreswan.config
      create: true
      mode: 420
    when: ( "libreswan" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )
    tags:
    - CCE-83446-5
    - DISA-STIG-RHEL-09-671020
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - PCI-DSS-Req-2.2
    - configure_libreswan_crypto_policy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure Libreswan to use System Crypto Policy'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Libreswan to use System Crypto Policy'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_configure_libreswan_crypto_policy | default(true)
    | bool)
- block:
  - name: Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Get database modification
      time for distro
    ansible.builtin.stat:
      path: /etc/dconf/db/distro
    register: distro_db
    when:
    - '"gdm" in ansible_facts.packages'
    - '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-87295-2
    - DISA-STIG-RHEL-09-271090
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - dconf_db_up_to_date
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make sure that the dconf databases are up-to-date with regards to respective
      keyfiles - Get database modification time for distro'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Get database
        modification time for distro'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_db_up_to_date | default(true) | bool)
- block:
  - name: Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Get keyfiles for distro
    ansible.builtin.find:
      paths: /etc/dconf/db/distro.d/
    register: distro_keyfiles
    when:
    - '"gdm" in ansible_facts.packages'
    - '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-87295-2
    - DISA-STIG-RHEL-09-271090
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - dconf_db_up_to_date
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make sure that the dconf databases are up-to-date with regards to respective
      keyfiles - Get keyfiles for distro'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Get keyfiles
        for distro'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_db_up_to_date | default(true) | bool)
- block:
  - name: Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Run dconf update for distro
    ansible.builtin.command:
      cmd: dconf update
    when:
    - '"gdm" in ansible_facts.packages'
    - '"kernel-core" in ansible_facts.packages'
    - not (distro_db is not skipped and distro_db.stat.exists) or distro_keyfiles.files | length > 0 and distro_keyfiles.files
      | map(attribute='mtime') | max > (distro_db is not skipped and distro_db.stat.mtime)
    tags:
    - CCE-87295-2
    - DISA-STIG-RHEL-09-271090
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - dconf_db_up_to_date
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make sure that the dconf databases are up-to-date with regards to respective
      keyfiles - Run dconf update for distro'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Run dconf
        update for distro'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_db_up_to_date | default(true) | bool)
- block:
  - name: Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Get database modification
      time for local
    ansible.builtin.stat:
      path: /etc/dconf/db/local
    register: local_db
    when:
    - '"gdm" in ansible_facts.packages'
    - '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-87295-2
    - DISA-STIG-RHEL-09-271090
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - dconf_db_up_to_date
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make sure that the dconf databases are up-to-date with regards to respective
      keyfiles - Get database modification time for local'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Get database
        modification time for local'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_db_up_to_date | default(true) | bool)
- block:
  - name: Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Get keyfiles for local
    ansible.builtin.find:
      paths: /etc/dconf/db/local.d/
    register: local_keyfiles
    when:
    - '"gdm" in ansible_facts.packages'
    - '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-87295-2
    - DISA-STIG-RHEL-09-271090
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - dconf_db_up_to_date
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make sure that the dconf databases are up-to-date with regards to respective
      keyfiles - Get keyfiles for local'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Get keyfiles
        for local'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_db_up_to_date | default(true) | bool)
- block:
  - name: Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Run dconf update for local
    ansible.builtin.command:
      cmd: dconf update
    when:
    - '"gdm" in ansible_facts.packages'
    - '"kernel-core" in ansible_facts.packages'
    - not (local_db is not skipped and local_db.stat.exists) or local_keyfiles.files | length > 0 and local_keyfiles.files
      | map(attribute='mtime') | max > (local_db is not skipped and local_db.stat.mtime)
    tags:
    - CCE-87295-2
    - DISA-STIG-RHEL-09-271090
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - dconf_db_up_to_date
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make sure that the dconf databases are up-to-date with regards to respective
      keyfiles - Run dconf update for local'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make sure that the dconf databases are up-to-date with regards to respective keyfiles - Run dconf
        update for local'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_db_up_to_date | default(true) | bool)
- block:
  - name: Disable the GNOME3 Login Restart and Shutdown Buttons
    community.general.ini_file:
      dest: /etc/dconf/db/distro.d/00-security-settings
      section: org/gnome/login-screen
      option: disable-restart-buttons
      value: 'true'
      create: true
      no_extra_spaces: true
      mode: 420
    register: result_ini
    when: '"gdm" in ansible_facts.packages'
    tags:
    - CCE-86315-9
    - DISA-STIG-RHEL-09-271095
    - DISA-STIG-RHEL-09-271100
    - NIST-800-171-3.1.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_restart_shutdown
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable the GNOME3 Login Restart and Shutdown Buttons'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable the GNOME3 Login Restart and Shutdown Buttons'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_restart_shutdown | default(true)
    | bool)
- block:
  - name: Prevent user modification of GNOME disablement of Login Restart and Shutdown Buttons
    ansible.builtin.lineinfile:
      path: /etc/dconf/db/distro.d/locks/00-security-settings-lock
      regexp: ^/org/gnome/login-screen/disable-restart-buttons
      line: /org/gnome/login-screen/disable-restart-buttons
      create: true
      mode: 420
    register: result_lineinfile
    when: '"gdm" in ansible_facts.packages'
    tags:
    - CCE-86315-9
    - DISA-STIG-RHEL-09-271095
    - DISA-STIG-RHEL-09-271100
    - NIST-800-171-3.1.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_restart_shutdown
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Prevent user modification of GNOME disablement of Login Restart and Shutdown
      Buttons'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Prevent user modification of GNOME disablement of Login Restart and Shutdown Buttons'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_restart_shutdown | default(true)
    | bool)
- block:
  - name: Dconf Update
    ansible.builtin.command: dconf update
    when:
    - '"gdm" in ansible_facts.packages'
    - result_ini is changed or result_lineinfile is changed
    tags:
    - CCE-86315-9
    - DISA-STIG-RHEL-09-271095
    - DISA-STIG-RHEL-09-271100
    - NIST-800-171-3.1.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_restart_shutdown
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Dconf Update'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Dconf Update'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_restart_shutdown | default(true)
    | bool)
- block:
  - name: Disable GDM Automatic Login
    community.general.ini_file:
      dest: /etc/gdm/custom.conf
      section: daemon
      option: AutomaticLoginEnable
      value: 'false'
      no_extra_spaces: true
      create: true
      mode: 420
    when: '"gdm" in ansible_facts.packages'
    tags:
    - CCE-89663-9
    - DISA-STIG-RHEL-09-271040
    - NIST-800-171-3.1.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - gnome_gdm_disable_automatic_login
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable GDM Automatic Login'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable GDM Automatic Login'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_gnome_gdm_disable_automatic_login | default(true)
    | bool)
- block:
  - name: Disable Ctrl-Alt-Del Reboot Key Sequence in GNOME3
    community.general.ini_file:
      dest: /etc/dconf/db/local.d/00-security-settings
      section: org/gnome/settings-daemon/plugins/media-keys
      option: logout
      value: '['''']'
      create: true
      no_extra_spaces: true
      mode: 420
    register: result_ini
    when: '"gdm" in ansible_facts.packages'
    tags:
    - CCE-88653-1
    - DISA-STIG-RHEL-09-271105
    - DISA-STIG-RHEL-09-271110
    - NIST-800-171-3.1.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_ctrlaltdel_reboot
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Ctrl-Alt-Del Reboot Key Sequence in GNOME3'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Ctrl-Alt-Del Reboot Key Sequence in GNOME3'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_ctrlaltdel_reboot | default(true)
    | bool)
- block:
  - name: Prevent user modification of GNOME disablement of Ctrl-Alt-Del
    ansible.builtin.lineinfile:
      path: /etc/dconf/db/local.d/locks/00-security-settings-lock
      regexp: ^/org/gnome/settings-daemon/plugins/media-keys/logout$
      line: /org/gnome/settings-daemon/plugins/media-keys/logout
      create: true
      mode: 420
    register: result_lineinfile
    when: '"gdm" in ansible_facts.packages'
    tags:
    - CCE-88653-1
    - DISA-STIG-RHEL-09-271105
    - DISA-STIG-RHEL-09-271110
    - NIST-800-171-3.1.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_ctrlaltdel_reboot
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Prevent user modification of GNOME disablement of Ctrl-Alt-Del'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Prevent user modification of GNOME disablement of Ctrl-Alt-Del'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_ctrlaltdel_reboot | default(true)
    | bool)
- block:
  - name: Dconf Update
    ansible.builtin.command: dconf update
    when:
    - '"gdm" in ansible_facts.packages'
    - result_ini is changed or result_lineinfile is changed
    tags:
    - CCE-88653-1
    - DISA-STIG-RHEL-09-271105
    - DISA-STIG-RHEL-09-271110
    - NIST-800-171-3.1.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(b)
    - dconf_gnome_disable_ctrlaltdel_reboot
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Dconf Update'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Dconf Update'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_dconf_gnome_disable_ctrlaltdel_reboot | default(true)
    | bool)
- block:
  - name: Ensure GPG check is globally activated
    community.general.ini_file:
      dest: /etc/dnf/dnf.conf
      section: main
      option: gpgcheck
      value: 1
      no_extra_spaces: true
      create: false
    when: '"dnf" in ansible_facts.packages'
    tags:
    - CCE-83457-2
    - CJIS-5.10.4.1
    - DISA-STIG-RHEL-09-214015
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-11(a)
    - NIST-800-53-CM-11(b)
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SA-12
    - NIST-800-53-SA-12(10)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - configure_strategy
    - ensure_gpgcheck_globally_activated
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure GPG check is globally activated'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure GPG check is globally activated'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_gpgcheck_globally_activated | default(true)
    | bool)
- block:
  - name: Ensure GPG check Enabled for Local Packages (dnf)
    block:
    - name: Check stats of dnf
      ansible.builtin.stat:
        path: /etc/dnf/dnf.conf
      register: pkg
    - name: Check if config file of dnf is a symlink
      ansible.builtin.set_fact:
        pkg_config_file_symlink: '{{ pkg.stat.lnk_target if pkg.stat.lnk_target is match("^/.*") else "/etc/dnf/dnf.conf"
          | dirname ~ "/" ~ pkg.stat.lnk_target }}'
      when: (pkg is not skipped and pkg.stat.lnk_target) is defined
    - name: Ensure GPG check Enabled for Local Packages (dnf)
      community.general.ini_file:
        dest: '{{ pkg_config_file_symlink |  default("/etc/dnf/dnf.conf") }}'
        section: main
        option: localpkg_gpgcheck
        value: 1
        no_extra_spaces: true
        create: true
        mode: 420
    when: '"dnf" in ansible_facts.packages'
    tags:
    - CCE-83463-0
    - DISA-STIG-RHEL-09-214020
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-11(a)
    - NIST-800-53-CM-11(b)
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SA-12
    - NIST-800-53-SA-12(10)
    - ensure_gpgcheck_local_packages
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure GPG check Enabled for Local Packages (dnf)'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure GPG check Enabled for Local Packages (dnf)'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_gpgcheck_local_packages | default(true) | bool)
- block:
  - name: Grep for dnf repo section names
    ansible.builtin.shell: 'set -o pipefail

      grep -HEr ''^\[.+\]'' -r /etc/yum.repos.d/

      '
    register: repo_grep_results
    failed_when: repo_grep_results.rc not in [0, 1]
    changed_when: false
    tags:
    - CCE-83464-8
    - CJIS-5.10.4.1
    - DISA-STIG-RHEL-09-214025
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-11(a)
    - NIST-800-53-CM-11(b)
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SA-12
    - NIST-800-53-SA-12(10)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - enable_strategy
    - ensure_gpgcheck_never_disabled
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Grep for dnf repo section names'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Grep for dnf repo section names'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_gpgcheck_never_disabled | default(true) | bool)
- block:
  - name: Set gpgcheck=1 for each dnf repo
    community.general.ini_file:
      path: '{{ item[0] }}'
      section: '{{ item[1] }}'
      option: gpgcheck
      value: '1'
      no_extra_spaces: true
    loop: '{{ repo_grep_results.stdout |regex_findall( ''(.+\.repo):\[(.+)\]\n?'' ) if repo_grep_results is not skipped else
      [] }}'
    when: repo_grep_results is not skipped
    tags:
    - CCE-83464-8
    - CJIS-5.10.4.1
    - DISA-STIG-RHEL-09-214025
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-11(a)
    - NIST-800-53-CM-11(b)
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SA-12
    - NIST-800-53-SA-12(10)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - enable_strategy
    - ensure_gpgcheck_never_disabled
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set gpgcheck=1 for each dnf repo'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set gpgcheck=1 for each dnf repo'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_gpgcheck_never_disabled | default(true) | bool)
- block:
  - name: 'Ensure Red Hat GPG Key Installed: Read permission of GPG key directory'
    ansible.builtin.stat:
      path: /etc/pki/rpm-gpg/
    register: gpg_key_directory_permission
    check_mode: false
    tags:
    - CCE-84180-9
    - CJIS-5.10.4.1
    - DISA-STIG-RHEL-09-214010
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Red Hat GPG Key Installed: Read permission of GPG key directory'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Red Hat GPG Key Installed: Read permission of GPG key directory'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_redhat_gpgkey_installed | default(true) | bool)
- block:
  - name: 'Ensure Red Hat GPG Key Installed: Read signatures in GPG key'
    ansible.builtin.command: gpg --show-keys --with-fingerprint --with-colons "/etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release"
    changed_when: false
    register: gpg_fingerprints
    failed_when: false
    check_mode: false
    tags:
    - CCE-84180-9
    - CJIS-5.10.4.1
    - DISA-STIG-RHEL-09-214010
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Red Hat GPG Key Installed: Read signatures in GPG key'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Red Hat GPG Key Installed: Read signatures in GPG key'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_redhat_gpgkey_installed | default(true) | bool)
- block:
  - name: 'Ensure Red Hat GPG Key Installed: Set Fact - Installed GPG Fingerprints'
    ansible.builtin.set_fact:
      gpg_installed_fingerprints: '{{ gpg_fingerprints.stdout | regex_findall(''^pub.*\n(?:^fpr[:]*)([0-9A-Fa-f]*)'', ''\1'')
        | list }}'
    tags:
    - CCE-84180-9
    - CJIS-5.10.4.1
    - DISA-STIG-RHEL-09-214010
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Red Hat GPG Key Installed: Set Fact - Installed GPG Fingerprints''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Red Hat GPG Key Installed: Set Fact - Installed GPG Fingerprints'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_redhat_gpgkey_installed | default(true) | bool)
- block:
  - name: 'Ensure Red Hat GPG Key Installed: Set Fact - Valid fingerprints'
    ansible.builtin.set_fact:
      gpg_valid_fingerprints:
      - 567E347AD0044ADE55BA8A5F199E2F91FD431D51
      - 7E4624258C406535D56D6F135054E4A45A6340B3
    tags:
    - CCE-84180-9
    - CJIS-5.10.4.1
    - DISA-STIG-RHEL-09-214010
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Red Hat GPG Key Installed: Set Fact - Valid fingerprints'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Red Hat GPG Key Installed: Set Fact - Valid fingerprints'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_redhat_gpgkey_installed | default(true) | bool)
- block:
  - name: 'Ensure Red Hat GPG Key Installed: Import RedHat GPG key'
    ansible.builtin.command: rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release
    when:
    - (gpg_key_directory_permission is not skipped and gpg_key_directory_permission.stat.mode) <= '0755'
    - (gpg_installed_fingerprints | difference(gpg_valid_fingerprints)) | length == 0
    - gpg_installed_fingerprints | length > 0
    - ansible_distribution == "RedHat"
    tags:
    - CCE-84180-9
    - CJIS-5.10.4.1
    - DISA-STIG-RHEL-09-214010
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Red Hat GPG Key Installed: Import RedHat GPG key'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Red Hat GPG Key Installed: Import RedHat GPG key'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_ensure_redhat_gpgkey_installed | default(true) | bool)
- block:
  - name: Disable Ctrl-Alt-Del Burst Action
    ansible.builtin.lineinfile:
      dest: /etc/systemd/system.conf
      state: present
      regexp: ^CtrlAltDelBurstAction
      line: CtrlAltDelBurstAction=none
      create: true
      mode: 420
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"systemd" in ansible_facts.packages'
    tags:
    - CCE-90308-8
    - DISA-STIG-RHEL-09-211045
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - disable_ctrlaltdel_burstaction
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Disable Ctrl-Alt-Del Burst Action'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Ctrl-Alt-Del Burst Action'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_disable_ctrlaltdel_burstaction | default(true) | bool)
- block:
  - name: Disable Ctrl-Alt-Del Reboot Activation
    ansible.builtin.systemd:
      name: ctrl-alt-del.target
      force: true
      masked: true
      state: stopped
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86667-3
    - DISA-STIG-RHEL-09-211050
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - disable_ctrlaltdel_reboot
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Disable Ctrl-Alt-Del Reboot Activation'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Ctrl-Alt-Del Reboot Activation'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_disable_ctrlaltdel_reboot | default(true) | bool)
- block:
  - name: Prevent Login to Accounts With Empty Password - Check if system relies on authselect
    ansible.builtin.stat:
      path: /usr/bin/authselect
    register: result_authselect_present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83611-4
    - CJIS-5.5.2
    - DISA-STIG-RHEL-09-611025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - configure_strategy
    - high_severity
    - low_complexity
    - medium_disruption
    - no_empty_passwords
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Prevent Login to Accounts With Empty Password - Check if system relies on
      authselect'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Prevent Login to Accounts With Empty Password - Check if system relies on authselect'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_empty_passwords | default(true) | bool)
- block:
  - name: Prevent Login to Accounts With Empty Password - Remediate using authselect
    block:
    - name: Prevent Login to Accounts With Empty Password - Check integrity of authselect current profile
      ansible.builtin.command:
        cmd: authselect check
      register: result_authselect_check_cmd
      changed_when: false
      check_mode: false
      failed_when: false
    - name: Prevent Login to Accounts With Empty Password - Informative message based on the authselect integrity check result
      ansible.builtin.assert:
        that:
        - ansible_check_mode or result_authselect_check_cmd.rc == 0
        fail_msg:
        - authselect integrity check failed. Remediation aborted!
        - This remediation could not be applied because an authselect profile was not selected or the selected profile is
          not intact.
        - It is not recommended to manually edit the PAM files when authselect tool is available.
        - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is recommended.
        success_msg:
        - authselect integrity check passed
    - name: Prevent Login to Accounts With Empty Password - Get authselect current features
      ansible.builtin.shell:
        cmd: authselect current | tail -n+3 | awk '{ print $2 }'
      register: result_authselect_features
      changed_when: false
      check_mode: false
      when:
      - result_authselect_check_cmd is success
    - name: Prevent Login to Accounts With Empty Password - Ensure "without-nullok" feature is enabled using authselect tool
      ansible.builtin.command:
        cmd: authselect enable-feature without-nullok
      register: result_authselect_enable_feature_cmd
      when:
      - result_authselect_check_cmd is success
      - result_authselect_features.stdout is not search("without-nullok")
    - name: Prevent Login to Accounts With Empty Password - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - result_authselect_enable_feature_cmd is not skipped
      - result_authselect_enable_feature_cmd is success
    when:
    - '"kernel-core" in ansible_facts.packages'
    - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - CCE-83611-4
    - CJIS-5.5.2
    - DISA-STIG-RHEL-09-611025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - configure_strategy
    - high_severity
    - low_complexity
    - medium_disruption
    - no_empty_passwords
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Prevent Login to Accounts With Empty Password - Remediate using authselect''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Prevent Login to Accounts With Empty Password - Remediate using authselect'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_empty_passwords | default(true) | bool)
- block:
  - name: Prevent Login to Accounts With Empty Password - Remediate directly editing PAM files
    ansible.builtin.replace:
      dest: '{{ item }}'
      regexp: nullok
    loop:
    - /etc/pam.d/system-auth
    - /etc/pam.d/password-auth
    when:
    - '"kernel-core" in ansible_facts.packages'
    - not (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - CCE-83611-4
    - CJIS-5.5.2
    - DISA-STIG-RHEL-09-611025
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - configure_strategy
    - high_severity
    - low_complexity
    - medium_disruption
    - no_empty_passwords
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Prevent Login to Accounts With Empty Password - Remediate directly editing
      PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Prevent Login to Accounts With Empty Password - Remediate directly editing PAM files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_empty_passwords | default(true) | bool)
- block:
  - name: Collect users with no password
    ansible.builtin.command: 'awk -F: ''!$2 {print $1}'' /etc/shadow

      '
    register: users_nopasswd
    changed_when: false
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-85972-8
    - DISA-STIG-RHEL-09-611155
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.2
    - high_severity
    - low_complexity
    - low_disruption
    - no_empty_passwords_etc_shadow
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with no password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with no password'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_empty_passwords_etc_shadow | default(true) | bool)
- block:
  - name: Lock users with no password
    ansible.builtin.command: 'passwd -l {{ item }}

      '
    with_items: '{{ users_nopasswd.stdout_lines }}'
    when:
    - '"kernel-core" in ansible_facts.packages'
    - users_nopasswd is not skipped and users_nopasswd.stdout_lines | length > 0
    tags:
    - CCE-85972-8
    - DISA-STIG-RHEL-09-611155
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.2
    - high_severity
    - low_complexity
    - low_disruption
    - no_empty_passwords_etc_shadow
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock users with no password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock users with no password'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_empty_passwords_etc_shadow | default(true) | bool)
- block:
  - name: Get all /etc/passwd file entries
    ansible.builtin.getent:
      database: passwd
      split: ':'
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-83624-7
    - DISA-STIG-RHEL-09-411100
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-6(5)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-4(b)
    - PCI-DSS-Req-8.5
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.1
    - accounts_no_uid_except_zero
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all /etc/passwd file entries'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all /etc/passwd file entries'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_accounts_no_uid_except_zero | default(true) | bool)
- block:
  - name: Lock the password of the user accounts other than root with uid 0
    ansible.builtin.command: passwd -l {{ item.key }}
    loop: '{{ getent_passwd | dict2items | rejectattr(''key'', ''equalto'', ''root'') | list }}'
    when:
    - '"kernel-core" in ansible_facts.packages'
    - item.value.1  == '0'
    tags:
    - CCE-83624-7
    - DISA-STIG-RHEL-09-411100
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-6(5)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-4(b)
    - PCI-DSS-Req-8.5
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.1
    - accounts_no_uid_except_zero
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock the password of the user accounts other than root with uid 0'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock the password of the user accounts other than root with uid 0'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_accounts_no_uid_except_zero | default(true) | bool)
- block:
  - name: Ensure SELinux State is Enforcing - Check current SELinux state
    ansible.builtin.command:
      cmd: getenforce
    register: current_selinux_state
    check_mode: false
    changed_when: false
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-84079-3
    - DISA-STIG-RHEL-09-431010
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - NIST-800-53-AU-9
    - NIST-800-53-SC-7(21)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - selinux_state
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux State is Enforcing - Check current SELinux state'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux State is Enforcing - Check current SELinux state'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_selinux_state | default(true) | bool)
- block:
  - name: Ensure SELinux State is Enforcing
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/selinux/config
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/selinux/config
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        line: SELINUX={{ var_selinux_state }}
        state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-84079-3
    - DISA-STIG-RHEL-09-431010
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - NIST-800-53-AU-9
    - NIST-800-53-SC-7(21)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - selinux_state
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux State is Enforcing'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux State is Enforcing'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_selinux_state | default(true) | bool)
- block:
  - name: Ensure SELinux State is Enforcing - Mark system to relabel SELinux on next boot
    ansible.builtin.file:
      path: /.autorelabel
      state: touch
      access_time: preserve
      modification_time: preserve
      mode: 420
    when:
    - '"kernel-core" in ansible_facts.packages'
    - current_selinux_state.stdout | lower != var_selinux_state
    tags:
    - CCE-84079-3
    - DISA-STIG-RHEL-09-431010
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - NIST-800-53-AU-9
    - NIST-800-53-SC-7(21)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - selinux_state
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux State is Enforcing - Mark system to relabel SELinux on next
      boot'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux State is Enforcing - Mark system to relabel SELinux on next boot'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_selinux_state | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Define Excluded (Non-Local) File Systems and Paths
    ansible.builtin.set_fact:
      excluded_fstypes:
      - afs
      - autofs
      - ceph
      - cifs
      - smb3
      - smbfs
      - sshfs
      - ncpfs
      - ncp
      - nfs
      - nfs4
      - gfs
      - gfs2
      - glusterfs
      - gpfs
      - pvfs2
      - ocfs2
      - lustre
      - davfs
      - fuse.sshfs
      excluded_paths:
      - dev
      - proc
      - run
      - sys
      search_paths: []
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Define Excluded (Non-Local) File
      Systems and Paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Define Excluded (Non-Local) File Systems and Paths'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Find Relevant Root Directories Ignoring Pre-Defined Excluded Paths
    ansible.builtin.find:
      paths: /
      file_type: directory
      excludes: '{{ excluded_paths }}'
      hidden: true
      recurse: false
    register: result_relevant_root_dirs
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Find Relevant Root Directories Ignoring
      Pre-Defined Excluded Paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Find Relevant Root Directories Ignoring Pre-Defined Excluded
        Paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Include Relevant Root Directories in a List of Paths to be Searched
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.path]) }}'
    loop: '{{ result_relevant_root_dirs.files }}'
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Include Relevant Root Directories
      in a List of Paths to be Searched'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Include Relevant Root Directories in a List of Paths to
        be Searched'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Increment Search Paths List with Local Partitions Mount Points
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.mount]) }}'
    loop: '{{ ansible_mounts }}'
    when:
    - item.fstype not in excluded_fstypes
    - item.mount != '/'
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Increment Search Paths List with
      Local Partitions Mount Points'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Increment Search Paths List with Local Partitions Mount
        Points'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Increment Search Paths List with Local NFS File System Targets
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.device.split('':'')[1]]) }}'
    loop: '{{ ansible_mounts }}'
    when: item.device is search("localhost:")
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Increment Search Paths List with
      Local NFS File System Targets'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Increment Search Paths List with Local NFS File System Targets''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Define Rule Specific Facts
    ansible.builtin.set_fact:
      shosts_equiv_files:
      - /shosts.equiv
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Define Rule Specific Facts'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Define Rule Specific Facts'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Find All shosts.equiv Files in Local File Systems
    ansible.builtin.command:
      cmd: find {{ item }} -xdev -type f -name "shosts.equiv"
    loop: '{{ search_paths }}'
    changed_when: false
    register: result_found_shosts_equiv_files
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Find All shosts.equiv Files in Local
      File Systems'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Find All shosts.equiv Files in Local File Systems'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Create List of shosts.equiv Files Present in Local File Systems
    ansible.builtin.set_fact:
      shosts_equiv_files: '{{ shosts_equiv_files | union(item.stdout_lines) | list }}'
    loop: '{{ result_found_shosts_equiv_files.results }}'
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Create List of shosts.equiv Files
      Present in Local File Systems'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Create List of shosts.equiv Files Present in Local File
        Systems'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove Host-Based Authentication Files - Ensure No shosts.equiv Files Are Present in the System
    ansible.builtin.file:
      path: '{{ item }}'
      state: absent
    loop: '{{ shosts_equiv_files }}'
    tags:
    - CCE-90208-0
    - DISA-STIG-RHEL-09-252070
    - high_severity
    - low_complexity
    - low_disruption
    - no_host_based_files
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove Host-Based Authentication Files - Ensure No shosts.equiv Files Are
      Present in the System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove Host-Based Authentication Files - Ensure No shosts.equiv Files Are Present in the System''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Define Excluded (Non-Local) File Systems and Paths
    ansible.builtin.set_fact:
      excluded_fstypes:
      - afs
      - autofs
      - ceph
      - cifs
      - smb3
      - smbfs
      - sshfs
      - ncpfs
      - ncp
      - nfs
      - nfs4
      - gfs
      - gfs2
      - glusterfs
      - gpfs
      - pvfs2
      - ocfs2
      - lustre
      - davfs
      - fuse.sshfs
      excluded_paths:
      - dev
      - proc
      - run
      - sys
      search_paths: []
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Define Excluded (Non-Local)
      File Systems and Paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Define Excluded (Non-Local) File Systems and Paths''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Find Relevant Root Directories Ignoring Pre-Defined Excluded Paths
    ansible.builtin.find:
      paths: /
      file_type: directory
      excludes: '{{ excluded_paths }}'
      hidden: true
      recurse: false
    register: result_relevant_root_dirs
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Find Relevant Root Directories
      Ignoring Pre-Defined Excluded Paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Find Relevant Root Directories Ignoring Pre-Defined
        Excluded Paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Include Relevant Root Directories in a List of Paths to be Searched
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.path]) }}'
    loop: '{{ result_relevant_root_dirs.files }}'
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Include Relevant Root Directories
      in a List of Paths to be Searched'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Include Relevant Root Directories in a List of Paths
        to be Searched'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Increment Search Paths List with Local Partitions Mount Points
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.mount]) }}'
    loop: '{{ ansible_mounts }}'
    when:
    - item.fstype not in excluded_fstypes
    - item.mount != '/'
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Increment Search Paths List
      with Local Partitions Mount Points'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Increment Search Paths List with Local Partitions Mount
        Points'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Increment Search Paths List with Local NFS File System Targets
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.device.split('':'')[1]]) }}'
    loop: '{{ ansible_mounts }}'
    when: item.device is search("localhost:")
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Increment Search Paths List
      with Local NFS File System Targets'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Increment Search Paths List with Local NFS File System
        Targets'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Define Rule Specific Facts
    ansible.builtin.set_fact:
      user_shosts_files:
      - /.shosts
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Define Rule Specific Facts''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Define Rule Specific Facts'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Find All .shosts Files in Local File Systems
    ansible.builtin.command:
      cmd: find {{ item }} -xdev -type f -name ".shosts"
    loop: '{{ search_paths }}'
    changed_when: false
    register: result_found_shosts_files
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Find All .shosts Files in Local
      File Systems'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Find All .shosts Files in Local File Systems'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Create List of .shosts Files Present in Local File Systems
    ansible.builtin.set_fact:
      user_shosts_files: '{{ user_shosts_files | union(item.stdout_lines) | list }}'
    loop: '{{ result_found_shosts_files.results }}'
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Create List of .shosts Files
      Present in Local File Systems'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Create List of .shosts Files Present in Local File
        Systems'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Remove User Host-Based Authentication Files - Ensure No .shosts Files Are Present in the System
    ansible.builtin.file:
      path: '{{ item }}'
      state: absent
    loop: '{{ user_shosts_files }}'
    tags:
    - CCE-86532-9
    - DISA-STIG-RHEL-09-252075
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_user_host_based_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove User Host-Based Authentication Files - Ensure No .shosts Files Are
      Present in the System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove User Host-Based Authentication Files - Ensure No .shosts Files Are Present in the System''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_no_user_host_based_files | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured in /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_has_parameter
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90799-8
    - CJIS-5.5.6
    - DISA-STIG-RHEL-09-255040
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords
      is configured in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured
        in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured in /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
    register: _config_dir_has_parameter
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90799-8
    - CJIS-5.5.6
    - DISA-STIG-RHEL-09-255040
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords
      is configured in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured
        in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if /etc/ssh/sshd_config exists
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: _config_file_exists
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90799-8
    - CJIS-5.5.6
    - DISA-STIG-RHEL-09-255040
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if /etc/ssh/sshd_config exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if /etc/ssh/sshd_config exists'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured correctly in
      /etc/ssh/sshd_config
    ansible.builtin.lineinfile:
      path: /etc/ssh/sshd_config
      regexp: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+no$
      state: absent
    check_mode: true
    changed_when: false
    register: _config_file_correctly
    when:
    - '"kernel-core" in ansible_facts.packages'
    - (_config_file_exists is not skipped and _config_file_exists.stat.exists)
    tags:
    - CCE-90799-8
    - CJIS-5.5.6
    - DISA-STIG-RHEL-09-255040
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords
      is configured correctly in /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured
        correctly in /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured correctly in
      /etc/ssh/sshd_config.d
    ansible.builtin.find:
      paths:
      - /etc/ssh/sshd_config.d
      contains: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+no$
    register: _config_dir_correctly
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90799-8
    - CJIS-5.5.6
    - DISA-STIG-RHEL-09-255040
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords
      is configured correctly in /etc/ssh/sshd_config.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - Check if the parameter PermitEmptyPasswords is configured
        correctly in /etc/ssh/sshd_config.d'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitEmptyPasswords is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files | default([]) }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched > 0
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        line: PermitEmptyPasswords no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when:
    - '"kernel-core" in ansible_facts.packages'
    - _config_file_correctly is not skipped
    - (_config_file_correctly.found == 0 and _config_dir_correctly.matched == 0) or ((_config_file_has_parameter.found | int)
      + (_config_dir_has_parameter.matched | int)) != 1
    tags:
    - CCE-90799-8
    - CJIS-5.5.6
    - DISA-STIG-RHEL-09-255040
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
      modification_time: preserve
      access_time: preserve
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90799-8
    - CJIS-5.5.6
    - DISA-STIG-RHEL-09-255040
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (rhel9stig_sshd_disable_empty_passwords | default(true) | bool)
